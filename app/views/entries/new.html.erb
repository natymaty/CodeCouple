<%= form_for Entry.new, html: { multipart: true } do |form| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@entry.errors.count, "error") %>
        Prohibited this article from being saved
      </h2>
      <ul>
        <% @entry.errors.full_messages.each do |msg| %>
          </li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= form.label("title") %></p>
  <p><%= form.text_field :title, class: 'form' %></p>
  <p><%= form.label("contents") %></p>
  <p><%= form.text_area :contents, class: 'formText' %></p>
  <p><%= form.label("author") %></p>
  <p><%= form.text_field :author, class: 'form' %></p>
  <p><%= form.label("picture") %></p>
  <p><%= form.file_field :avatar %></p>
  <p><%= form.label("youtube link") %></p>
  <p><%= form.text_field :youtube_url, class: 'form'  %></p>
  <p><%= form.submit %></p>
<% end %>
<p><%= link_to("Back to all entries", entries_path) %></p>
